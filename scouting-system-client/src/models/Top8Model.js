export default class Top8Model {
  constructor(name, avgSpeaker, avgAmp, ranking) {
    this.name = name;
    this.avgSpeaker = avgSpeaker;
    this.avgAmp = avgAmp;
    this.ranking = ranking;
  }
}
